test "simple test" {
    assert : true
    bool = true
    assert : bool

    obj = {hello: "world"}
    assert : obj.hello == "world"

    array = [1,2]
    assert : array.1 == 2

    num = 1
    assert : num == 1

    val = get : "http://localhost:8080/string"
    assert : val == "test-string"
}